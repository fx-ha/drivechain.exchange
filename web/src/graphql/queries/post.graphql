query Post($id: String!) {
  post(id: $id) {
    id
    depositChain
    depositAddress
    hasDeposited
    depositAmount
    coinNewsFee
    text
    txid
    topic {
      hex
      name
      createdAt
    }
    extra
    createdAt
    updatedAt
  }
}
