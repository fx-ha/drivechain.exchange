query FaucetRequests($limit: Int!, $cursor: String) {
  faucetRequests(limit: $limit, cursor: $cursor) {
    faucetRequests {
      id
      chain
      address
      amount
      isPaid
      txid
      createdAt
      updatedAt
    }
    hasMore
    total
  }
}
